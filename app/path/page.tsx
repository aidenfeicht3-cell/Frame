import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function PathPage() {
  return (
    <PagePlaceholder
      title="The Path"
      emoji="🧭"
      description="Your leveled journey through Seasons — small lessons plus one action each. This is built in a later step."
    />
  );
}
