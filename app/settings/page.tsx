import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      emoji="⚙️"
      description="Edit your brand kit, goal, and cadence — and paste your Anthropic API key when you're ready for real AI. Built in a later step."
    />
  );
}
