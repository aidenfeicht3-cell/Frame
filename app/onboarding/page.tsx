import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function OnboardingPage() {
  return (
    <PagePlaceholder
      title="Onboarding"
      emoji="🌱"
      description="Next up: we'll ask your niche and goal, then generate your starter brand kit. We build this in Step 2."
    />
  );
}
