import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      emoji="📅"
      description="Schedule upload dates, set a recurring cadence, and get gentle reminders. Built in a later step."
    />
  );
}
