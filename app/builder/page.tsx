import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function BuilderPage() {
  return (
    <PagePlaceholder
      title="Video Builder"
      emoji="🎬"
      description="The core loop: pick an idea, then reveal your production plan one step at a time. We build this in Step 3."
    />
  );
}
